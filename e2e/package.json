{
  "name": "@cosmos/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:gate": "playwright test --grep-invert @perf",
    "test:perf": "playwright test --grep @perf",
    "test:ui": "playwright test --ui",
    "update-baselines": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^22.0.0"
  }
}
